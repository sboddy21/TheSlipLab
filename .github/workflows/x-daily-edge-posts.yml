name: The Slip Lab Daily Edge Posts

on:
  schedule:
    # 11:20 a.m. Eastern window. Posts live when X_DRY_RUN is false.
    - cron: "20 15 * * *"
    - cron: "20 16 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-edge-posts
  cancel-in-progress: false

jobs:
  build-daily-edge-posts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync selected branch
        run: git pull origin "${GITHUB_REF_NAME}" --rebase

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Guard 11 a.m. Eastern window
        run: |
          EASTERN_HOUR=$(node -e "console.log(new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',hour:'numeric',hourCycle:'h23'}).format(new Date()))")
          if [ "$EASTERN_HOUR" != "11" ]; then
            echo "Not the 11 a.m. Eastern daily edge post window. Hour=$EASTERN_HOUR"
            echo "RUN_DAILY_EDGE_POSTS=false" >> "$GITHUB_ENV"
            exit 0
          fi

          echo "RUN_DAILY_EDGE_POSTS=true" >> "$GITHUB_ENV"
          echo "Building daily edge posts. Hour=$EASTERN_HOUR"

      - name: Refresh current MLB inputs
        if: env.RUN_DAILY_EDGE_POSTS == 'true'
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: node scripts/run_fast_refresh.js

      - name: Build current game context
        if: env.RUN_DAILY_EDGE_POSTS == 'true'
        run: node scripts/mlb/build_context_factors.js

      - name: Build daily edge posts
        if: env.RUN_DAILY_EDGE_POSTS == 'true'
        run: node scripts/content/build_x_daily_edge_posts.js

      - name: Post daily edge posts
        if: env.RUN_DAILY_EDGE_POSTS == 'true'
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          X_DRY_RUN: ${{ secrets.X_DRY_RUN }}
        run: node scripts/content/post_x_daily_edge_posts.js

      - name: Commit daily edge post outputs
        if: env.RUN_DAILY_EDGE_POSTS == 'true' && success()
        run: |
          git config --global user.name "TheSlipLabBot"
          git config --global user.email "bot@thesliplab.com"
          git add website/data/content/x_daily_edge_posts.json website/data/content/x_post_history.json exports/content/x_daily_edge_posts.json exports/content/x_daily_edge_posts.txt || true
          git diff --cached --quiet || git commit -m "Build daily edge X post outputs"

      - name: Push updates
        if: env.RUN_DAILY_EDGE_POSTS == 'true' && success()
        run: |
          for i in 1 2 3; do
            git push origin HEAD:"${GITHUB_REF_NAME}" && exit 0
            echo "Push failed. Syncing and retrying..."
            git pull origin "${GITHUB_REF_NAME}" --rebase --autostash || true
            sleep 10
          done

          echo "Push failed after retries"
          exit 1
